{
  "name": "templ",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@8.14.3",
  "description": "A modern monorepo project template",
  "author": "Joydip Roy <joydipand@gmail.com> (https://github.com/rjoydip)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rjoydip/templ.git"
  },
  "sideEffects": true,
  "engines": {
    "node": ">=18",
    "npm": ">=8",
    "pnpm": ">=8",
    "shell": "bash",
    "yarn": ">=3"
  },
  "scripts": {
    "postinstall": "taze -r -w -i -f -l",
    "prebuild": "pnpm run clean",
    "build": "turbo run build",
    "postbuild": "pnpm run -C scripts check",
    "bench": "turbo run bench",
    "clean": "turbo run clean",
    "commit": "turbo run commit && pnpm run changelog && better-commits",
    "changelog": "changelogen --output",
    "eslint": "eslint -c eslint.config.js --color --cache --fix --cache-location .eslintcache .",
    "lint": "turbo run lint && pnpm run knip",
    "knip": "knip -c ./config/knip.ts --include files,dependencies",
    "release": "changelogen --output --release && npm publish && git push --follow-tags",
    "setup": "turbo run setup",
    "test": "turbo run test"
  },
  "workspaces": [
    "apps/**",
    "benchmark",
    "docs",
    "packages/**",
    "scripts",
    "tools/**"
  ],
  "devDependencies": {
    "@antfu/eslint-config": "2.6.3",
    "better-commits": "^1.11.0",
    "changelogen": "^0.5.5",
    "eslint": "^8.56.0",
    "knip": "^4.2.1",
    "pnpm": "^8.14.3",
    "taze": "^0.13.1",
    "turbo": "^1.11.3"
  }
}
