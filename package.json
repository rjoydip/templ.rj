{
  "name": "@templ/workspace",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@8.15.4",
  "description": "A modern monorepo project template",
  "author": "Joydip Roy <joydipand@gmail.com> (https://github.com/rjoydip)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rjoydip/templ.git"
  },
  "sideEffects": true,
  "workspaces": [
    "apps/**",
    "packages/**",
    "scripts",
    "generator"
  ],
  "engines": {
    "node": ">=18",
    "npm": ">=8",
    "pnpm": ">=8",
    "shell": "bash",
    "yarn": ">=3"
  },
  "scripts": {
    "prepare": "husky",
    "all": "ni && nr deps:update && nr build && nr lint && nr test",
    "build": "nr -C scripts clean && turbo run build && knip && nr -C scripts build:check && nr -r typecheck",
    "deps:update": "taze -r -w -i -f -l",
    "lint": "eslint . --fix && nr -C scripts automd",
    "release": "changeset version && npm publish && git push --follow-tags",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@antfu/eslint-config": "2.6.4",
    "@antfu/ni": "^0.21.12",
    "@changesets/cli": "^2.27.1",
    "eslint": "^8.57.0",
    "husky": "^9.0.11",
    "knip": "^5.0.2",
    "pnpm": "^8.15.4",
    "taze": "^0.13.3",
    "turbo": "^1.12.4"
  }
}
