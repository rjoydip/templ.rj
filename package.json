{
  "private": true,
  "name": "templ",
  "version": "0.0.0",
  "description": "A project template",
  "license": "MIT",
  "author": "Joydip Roy <joydipand@gmail.com> (https://github.com/rjoydip)",
  "repository": "rjoydip/templ",
  "type": "module",
  "scripts": {
    "all": "pnpm clean && pnpm deps:fresh && pnpm build && pnpm changelog && pnpm size:limit && pnpm lint && pnpm test && pnpm test:cli && pnpm update:third_party",
    "build": "pnpm --filter './packages/**' run build",
    "docs:build": "pnpm -r docs:build",
    "coverage:lcov": "pnpm -C tools/scripts coverage:lcov",
    "clean": "rimraf .pnpm-store coverage && pnpm -r clean",
    "clean:nm": "pnpm -C tools/scripts clean:nm",
    "changelog": "auto-changelog -p",
    "create:pkg": "pnpm -C tools/scripts create:pkg",
    "deps:fresh": "taze -r -w -i",
    "preinstall": "npx only-allow pnpm",
    "lint": "pnpm --filter './packages/**' lint && pnpm lint:spell && pnpm lint:secret && pnpm lint:md",
    "lint:md": "pnpm -C tools/scripts lint:md",
    "lint:secret": "secretlint --secretlintignore .gitignore \"**/*\"",
    "lint:spell": "cspell .",
    "prepare": "husky install",
    "size:limit": "pnpm --filter './packages/**' size:limit",
    "test": "pnpm --filter './packages/**' test",
    "test:cli": "pnpm --filter './packages/**/cli' test:cli",
    "system:info": "envinfo",
    "update:third_party": "git submodule update --recursive --remote"
  },
  "sideEffects": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "third_party/*",
    "tools/*"
  ],
  "devDependencies": {
    "@commitlint/cli": "^17.7.1",
    "@commitlint/config-conventional": "^17.7.0",
    "@secretlint/secretlint-rule-preset-recommend": "^7.0.7",
    "auto-changelog": "^2.4.0",
    "cspell": "^7.3.6",
    "envinfo": "^7.10.0",
    "husky": "^8.0.3",
    "nano-staged": "^0.8.0",
    "pnpm": "^8.7.5",
    "rimraf": "^5.0.1",
    "secretlint": "^7.0.7",
    "snyk": "^1.1220.0",
    "stylelint": "^15.10.3",
    "taze": "^0.11.2"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=8",
    "shell": "bash",
    "yarn": "This project is configured to use pnpm"
  },
  "auto-changelog": {
    "output": "CHANGELOG.md",
    "template": "keepachangelog",
    "unreleased": true,
    "commitLimit": false
  },
  "nano-staged": {
    "*": [
      "cspell .",
      "secretlint \"**/*\""
    ],
    "*.{js,ts,jsx,tsx,md,json}": "prettier --write --cache .",
    "*.css": [
      "stylelint",
      "eslint --fix"
    ],
    "*.md": [
      "pnpm -C tools/scripts lint:md"
    ]
  },
  "packageManager": "pnpm@8.7.5",
  "stackblitz": {
    "startCommand": "pnpm dev"
  }
}
