/**
 * The Templ function is a React component that renders a div element with the text "TEMPL".
 * @returns A div element with the text "TEMPL" inside.
 */
export function Templ() {
  return <div>TEMPL</div>
}
