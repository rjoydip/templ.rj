/* The code `export const CLI_VERSION = '0.0.1'` is exporting a constant variable named `CLI_VERSION`
with the value `'0.0.1'`. This means that other modules or files can import this constant and use
its value. */
export const CLI_VERSION = '0.0.1'
