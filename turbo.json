{
  "$schema": "https://turborepo.org/schema.json",
  "baseBranch": "origin/main",
  "pipeline": {
    "dev": {
      "cache": false,
      "dependsOn": ["^dev", "^build"]
    },
    "docs:build": {
      "outputs": ["docs/**"],
      "outputMode": "new-only"
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "outputMode": "new-only"
    }
  }
}
