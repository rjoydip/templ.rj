coverage:
  status:
    project:
      default: off
      build:
        flags:
          - build
      cli:
        flags:
          - cli
      config:
        flags:
          - config
      core:
        flags:
          - core
      logger:
        flags:
          - logger
      utils:
        flags:
          - utils

flags:
  build:
    paths:
      - packages/build/
  cli:
    paths:
      - packages/cli/
  config:
    paths:
      - packages/config/
  core:
    paths:
      - packages/core/
  logger:
    paths:
      - packages/logger/
  utils:
    paths:
      - packages/utils/
